# Fixtures

`unity-minimal-clean` is a standalone copy of the minimal Unity fixture used to validate project detection, copy-set integrity, selector behavior and production preflight. Tests mutate only copies under a per-run external temporary directory.
